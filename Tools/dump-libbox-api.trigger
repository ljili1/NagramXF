Trigger file for the temporary "Dump libbox API" workflow.
Pushing a change to this file runs .github/workflows/dump-libbox-api.yml,
which downloads the sing-box libbox AAR and prints its Java API with javap.

Temporary — remove together with the workflow once the API is captured.
Run: 1
